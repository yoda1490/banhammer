# BanHammer environment configuration template
# Copy to .env and customize for your deployment

DB_HOST=localhost
DB_USER=fail2ban
DB_PASSWORD=fail2ban
DB_NAME=fail2ban
DB_TABLE=fail2ban
WEB_SERVER=ban.example.com

# Store the SHA-256 hash of your admin token here. Generate one with:
#   TOKEN=$(openssl rand -hex 32)
#   echo "Admin token: $TOKEN"
#   echo -n "$TOKEN" | sha256sum | awk '{print $1}'
ADMIN_TOKEN_HASH=
